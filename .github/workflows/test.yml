# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  first_build:
    # The type of runner that the job will run on
    runs-on: [ self-hosted, elastic, lineville@elastic-machines-multi-cluster.us-west-2.eksctl.io ]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: |
          echo Hello, from cluster lineville@elastic-machines-multi-cluster.us-west-2.eksctl.io!
          sleep 10
          echo Bye!
  # This workflow contains a single job called "build"
  second_build:
    # The type of runner that the job will run on
    runs-on: [ self-hosted, elastic, lineville@em-integration-testing.us-west-2.eksctl.io ]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: |
          echo Hello, from cluster lineville@em-integration-testing.us-west-2.eksctl.io!
          sleep 10
          echo Bye!
          
  third_build:
    # The type of runner that the job will run on
    runs-on: [ self-hosted, elastic, ci-cd-default-elastic-machines-dev-cluster ]

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: |
          echo Hello, from cluster ci-cd-default-elastic-machines-dev-cluster!
          sleep 10
          echo Bye!
